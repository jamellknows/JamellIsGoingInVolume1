class LinkedList {
    Node head; // head of the list
    // Linked list Node 
    class Node {
        int data;
        Node next;

        //Constructor to create a new node
        // Next is initialised as null by default
        Node(int d) { data = d;}
    }
}