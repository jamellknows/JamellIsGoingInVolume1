var head; // head of the list
// Linked List Node

class Node 
{
    //constructor to create a new node
    // next is initialised as null by default
    constructor(val) {
        this.data = val;
        this.next = null;
    }
}

